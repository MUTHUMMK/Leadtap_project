First create the Linux server - 
ubuntu OS 22.04 LTS

Login with ubuntu user=>
sudo apt update && sudo apt upgrade -y

1.Create the devops user to give the sudo access without password login(used SSH Key):

sudo useradd devops
sudo usermod -aG sudo devops

sudo passwd devops -(enter password)

Note:
#Human user (YOU):
SSH key login  
sudo with password
#Automation user (GitHub Action, Ansible, etc..):
passwordless sudo (restricted)

sudo nano /etc/ssh/sshd_config ->
"""
# Change default port (example: 222)
Port 222

# Disable root login
PermitRootLogin no

# Disable password login
PasswordAuthentication no

# Allow only specific user
AllowUsers devops
""" 

#SSH Key configure with devops user

sudo mkdir /home/devops/.ssh -p
sudo cp ~/.ssh/authorized_keys /home/devops/.ssh/

#Set Permission
sudo chown -R devops:devops /home/devops
sudo chown -R devops:devops /home/devops/.ssh
sudo chmod 700 /home/devops/.ssh
sudo chmod 600 /home/devops/.ssh/authorized_keys

sudo systemctl restart ssh

####################################################################################################

2.Configure UFW Firewall

bash

sudo apt install ufw -y

sudo ufw allow 222/tcp   # SSH (custom port)
sudo ufw allow 80/tcp     # HTTP
sudo ufw allow 443/tcp    # HTTPS

sudo ufw enable
sudo ufw status ->

"""
$ sudo ufw status
Status: active

To                         Action      From
--                         ------      ----
222/tcp                    ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
222/tcp (v6)               ALLOW       Anywhere (v6)
80/tcp (v6)                ALLOW       Anywhere (v6)
443/tcp (v6)               ALLOW       Anywhere (v6)

"""
#IPv6:

(v6) → Means IPv6 traffic allowed

Disable IPv6 in: 
sudo nano /etc/default/ufw 
"""
IPV6=no
"""

#Restrict SSH to Your IP:
sudo ufw allow from YOUR-IP to any port 2222 proto tcpsource ~/.bashrc

#Enable Rate Limiting for SSH:
sudo ufw limit 222/tcp

#Default Policies Check (Default: deny (incoming), allow (outgoing)):
sudo ufw status verbose

#Delete port:
sudo ufw delete allow 222/tcp

sudo ufw status numbered

#######################################################################################

3.Install Node.js 18+ using NVM

bash

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

ls -la ~/.nvm (Check if files created)

#####sudo cp /etc/skel/.bashrc /home/devops/ 

source ~/.bashrc

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

nvm -v

sudo nano ~/.bashrc ->
"""
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

"""

nvm -v


#Install Node.js 18
nvm install 18
nvm use 18
nvm alias default 18

#Verify Installation
node -v
npm -v

############################################################################################
#Deployment 
bash

git init
git clone https://github.com/Leadtap/lt-nilavan.git

cd lt-nilavan

npm install
npm run build 

npm start (testing) 

Note:
Enable & allow the 3000 port on EC2 Security & Server ufw also

#PM2 Install
npm install pm2 -g
pm2 start npm --name app_name -- start
pm2 status

#Enable Auto-Restart on Reboot
pm2 startup

sudo env PATH=$PATH:/home/devops/.nvm/versions/node/v18.20.8/bin /home/devops/.nvm/versions/node/v18.20.8/lib/node_modules/pm2/bin/pm2 startup systemd -u devops --hp /home/devops

pm2 save

#Ensure App Runs as Non-Root
ps aux | grep node	

#Check App Running on 3000 port (local server)
curl http://localhost:3000

#Check App Running on 3000 port (browser)
http://IP:3000 (or) http://localhost:3000 

#########################################################################################

#Nginx Install & Configuration & Reverse Proxy

Note:
Nginx config + Certbot require sudo (system-level changes)

#Install Nginx:
sudo apt update
sudo apt install nginx -y 

sudo systemctl enable nginx
sudo systemctl start nginx
sudo systemctl status nginx

#Reverse Proxy Config:
sudo nano /etc/nginx/sites-available/app.conf  ->
"""
server {
    listen 80;
    listen [::]:80;

    server_name YourDomain;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    #Block all hidden files
    location ~ /\.(?!well-known).* {
    deny all;
    }
}

"""

#Add Headers:
headers to protect against common web vulnerabilities

"""
add_header X-Frame-Options "SAMEORIGIN";
add_header X-Content-Type-Options "nosniff";
add_header X-XSS-Protection "1; mode=block";
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

"""

#Block all hidden files: (.git .env .htaccess other sensitive files )

"""
location ~ /\.(?!well-known).* {
    deny all;
}

"""

#Enable Site
sudo ln -s /etc/nginx/sites-available/app.conf /etc/nginx/sites-enabled/

sudo rm -r /etc/nginx/sites-enabled/default && sudo rm -r /etc/nginx/sites-available/default (default for nginx conf)

#Test & Restart Nginx
sudo nginx -t
sudo systemctl restart nginx

#Test HTTP Access

http://YourDomain (Runs on http protocol - not secure)

#Install Certbot (Let’s Encrypt)

sudo apt install certbot python3-certbot-nginx -y

#Get SSL Certificate (your domain)

Note:
Go to Your Domain Hosting Platform, select Domain -> Manage DNS records
Add => Type - A record with, Name - @, Point to - Your EC2 Server Public IP Address, TLL - 300

sudo certbot --nginx -d YourDomain

sudo systemctl restart nginx

#After Check your Browser: (HTTPS enabled)

https://YourDomain

curl -I https://YourDomain (in server cli)

curl -I https://YourDomain/.git (in server cli)


User → HTTPS (443)
        ↓
     Nginx
        ↓
   Domain (Next.js via PM2)
		

##############################################################################################################
###Testing Server

#Install SonarQube & Sonar Scanner 

docker run -d -p 9000:9000 --name sonarqube sonarqube:9.9-community

#Download sonar cli:

wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-8.0.1.6346-linux-x64.zip
unzip sonar-scanner-cli-8.0.1.6346-linux-x64.zip
mv sonar-scanner-cli-8.0.1.6346-linux-x64/* /opt/sonar-scanner

echo 'export PATH=$PATH:/opt/sonar-scanner/sonar-scanner-8.0.1.6346-linux-x64/bin' >> ~/.bashrc
source ~/.bashrc 

sonar-scanner -v

nano sonar-project.properties  ->
""" 
sonar.projectKey=nextjs-app
sonar.projectName=Security Scan
sonar.sources=.
sonar.exclusions=node_modules/**,.next/**,out/**,coverage/**
sonar.sourceEncoding=UTF-8
"""


sonar-scanner \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=sqa_9092d0be2a13877154fda24c83d0f142522eb5f2


#OWSAP ZAP:

sudo docker run --rm \
  --network host \
  -v "$(pwd):/zap/wrk" \
  ghcr.io/zaproxy/zaproxy:stable \
  zap-baseline.py \
  -t https://YourDomain/api/sendgrid \
  -r zap-report.html

docker run -it -p 8080:8080 ghcr.io/zaproxy/zaproxy:stable zap-webswing.sh




for i in $(seq 1 10); do curl -X POST https://YourDomain/api/sendgrid; done

curl -X POST https://YourDomain/api/sendgrid -d '{"message":"<script>alert(1)</script>"}'

curl -X GET https://YourDomain/api/sendgrid

curl -X POST https://YourDomain/api/sendgrid \
-H "Content-Type: application/json" \
-d '{"name":"test","email":"x@x.com","phone":"123","message":"<script>alert(1)</script>"}'


for i in $(seq 1 10); do 
curl -X POST https://YourDomain/api/sendgrid \
-H "Content-Type: application/json" \
-d '{"name":"test","email":"x@x.com","phone":"0000000000","message":"spam"}'
done

above all output is above => {"error":"SendGrid API key not configured"}

curl https://YourDomain/.git/HEAD -output  403 forbidden i secure the nginx conf block file to hidden all files libe .env.git all

curl -I https://YourDomain - output is (HTTP/1.1 200 OK
Server: nginx/1.18.0 (Ubuntu)
Date: Sat, 28 Mar 2026 14:54:23 GMT
Content-Type: text/html; charset=utf-8
Content-Length: 47445
Connection: keep-alive
Vary: RSC, Next-Router-State-Tree, Next-Router-Prefetch, Next-Router-Segment-Prefetch, Accept-Encoding
x-nextjs-cache: HIT
x-nextjs-prerender: 1
x-nextjs-stale-time: 4294967294
X-Powered-By: Next.js
Cache-Control: s-maxage=31536000,
ETag: "zecflpzaf610lu"
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Strict-Transport-Security: max-age=31536000; includeSubDomains
)

i attach the DAST Report .html in repostiry 

THIS FOR OUTPUT I WANT SECURITY_REPORT.md file




































